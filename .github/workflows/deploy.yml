name: Build and Deploy

on:
  push:
    branches: [ master ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u Nasif4547 --password-stdin

      - name: Build and Push Docker Image
        run: |
          docker build -t ghcr.io/Nasif4547/Inovec:latest .
          docker push ghcr.io/Nasif4547/Inovec:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_IP }}
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u Nasif4547 --password-stdin
            docker pull ghcr.io/Nasif4547/Inovec:latest
            docker rm -f inovec-app || true
            docker run -d --name inovec-app -p 3000:3000 \
              -e DATABASE_URL="${{ secrets.DATABASE_URL }}" \
              -e NEXTAUTH_SECRET="${{ secrets.NEXTAUTH_SECRET }}" \
              ghcr.io/Nasif4547/Inovec:latest
